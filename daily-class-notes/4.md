* Java app ... lets get the src
* 2 repos
    * maintain github action code
    * second repo will haev the java app code 
* `pull_request`
    * this event triggers a worklow when activity happens on a pull request .
    * By default pull_request triggers on three activities
        * opened => pr is created
        * synchronize => new commit is pushed to PR
        * reopened => Cloud PR is reopened
* `workflow_run`:
    this event triggers a workflow after another workflow.\
* `schedule`
    this schedule evernt trigers a workflow using a cron syntax.

Next Class: 
* runners: 
* without actions
* actions: 


github actions is different actions is different