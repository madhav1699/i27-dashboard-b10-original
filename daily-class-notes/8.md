* yesterday session summary:
    * env: static
        * workflow
        * job
        * step
    *  $GITHUB_ENV 
        * i can call/use from next step 
        * can i call/use in next job ? No 
    * variables and secrets 
        * vars.****
        * secrets.****
* Outputs
    * Step Ouputs 
    * Job Outputs
* In a workflow, jobs or steps often needs to share data with each other . 
* we have 2 ways to share data:
    * env:          -> Static values defined the yaml files
    * $GITHUB_ENV   -> dynaic values shared b.w steps in the same job 
* but what if you need to pass value from one job to other job ? 
* this is wehere outputs comes in 
    * step outputs  -> pass data b/w steps in the same job
    * job outputs   -> pass data b/w different JOBS 
* Basic example
```yaml
- name: MyStep
  id: my-steps # id is required 
  run: echo "name=siva" >> $GITHUB_OUTPUT

  # call
  ${{ steps.idname.outputs.keyname}}
```

* Job outputs example
```yaml
# The job sets the o/p
jobs:
  my-job:
    runs-on: ***
    steps:
    - name: first-step
      id: my-step
      run: echo "name=siva" >> $GITHUB_OUTPUT'
    outputs:
      my-key: ${{ steps.my-step.outputs.name}}


# this job needs to read the o/p
other-job:
  needs: my-job
  steps:
  - name: print data
    run: |
        echo ${{ needs.my-job.outputs.}}
```


## Context:
* ${{ context.property }}
* github.*
* runner.*
* env.*
* vars.*
* secrets.*
* steps.*
* needs.* 
* inputs.*
* matrix.*


on: 
  workflow_dispatch
    inputs: 

four input types;
string
on: 
  workflow_dispatch
    inputs: 
      INPUT_NAME:
        description: what is this input for ? 
        type: string or boolean or choice or number 
        default" 'default-value'

on: 
  workflow_dispatch
    inputs: 
      version:
        description: Which version to deploy
        type: string 
        default:  'v1.0.0'
boolean
on: 
  workflow_dispatch
    inputs: 
      skip-tests:
        description: skip tests ?
        type: boolean 
        default:  false
choice
on: 
  workflow_dispatch
    inputs: 
      environment:
        description: Which env u want to deploy
        type: choice
        options:
        - dev
        - test
        - stage
        default:  dev
number
  replicas:
    type: number
    descroption: number of replicas 
    default: 2

jenkins: parameters