## Self hosted runner 
* I will install java, maven, docker 
```bash
# update the packages
sudo apt-get update && sudo apt-get upgrade -y

# Install openjdk-21
sudo apt-get install -y openjdk-21-jdk

# Install maven 
sudo apt-get install -y maven

sudo apt-get install -y docker.io
sudo systemctl start docker
sudo systemctl enable  docker
sudo usermod -aG docker $USER
newgrp docker

#testing
./run

# 
sudo ./svc.sh install
sudo ./svc.sh start
sudo ./svc.sh status
```

self-hosted,linux,gcp


tetsing


github.com/i27org/auth-service.git
correct way
    access


github.com/siva/auth-service.git
wring way 




* concurrency

a developer pushes 3 commits quickly to main 
push 1 > run 1 starts (test + build + deploy) error1
push 2 > run 2 starts (test + build + deploy ) error 2
push 3 > run 3 starts (test + build _ deploy ) final 

Only one run at a time for this group 
what to do when a new run arrives when the old run is alrady running ?


concurrenncy:
    group: my-group-name # name of the concurrency group
    cancel-in-progress: true # what to do with the running job 



## Workflow
* Deployment 

>>>>>>>> short lived tokens
long lived tokens 

depend
Authentication (OIDC)
java app > cloud deploy 


Devops 

AI & ML 
    ML Ops 
    LLM Ops

DSA 

java or python 
DSA 
System desing 
AI ML 


LLMS AI Agents >

AI Ops 