## Type of runners :
* Github hosted Runners
* Self hosted runners.
## Self hosted runners:
* A self hosted runner is a machine that we provide , manage and registered with GITHUB so  it can recievd and execute workflow jobs, 

### Github hosted runners:
* ubuntu-latest
* windows-latest
* macos-latest
* Github provides the vm 
* githbus manage the vm,
* Every time we run a new job, a fresh vm will be spinnign. 
* We pay per minutes.

### Self hosted runners:
* We provide the vm (GCP, AWS, Azure, on-premises)
* We manage the underlying infra. IAAS 
* Persistent: files survive b/w runs
* You can pay for the vm.

### Runner Information 
* i27academy-runner
* us-central1
* e2-medium 
* boot disk ( ubuntu)
* Firewall http access (for app user)
* install requred s/w on the vm
    * java 
    * maven 
    * docker 
* Follow the steps provided by github to cofnigrure self hosted runner.
* Tomorrow lets do practically. 

* self hosted 
* OIDC > Security hardeingn
    * AWS, GCP 
* dependpbot
* OIDC > dashboard > Cloud 

keys X
svc account > key X


OIDC > Workload identity




terraform