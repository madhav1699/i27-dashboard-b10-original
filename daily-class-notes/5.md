## Day 5 
### Runners 
* A runner is a machine where your job executes. 
* Every job will be running in a seprate machine. 
    * In this the steps actually run. 
* when a new job comes a new runner will be coming
* inside the job > `runs-on` filed is mandatory. 
* runs-on: ubuntu-latest
* releavant comp: in jenkins we have slaves
#### Type of runner:
* Github hosted runners.
    * github provides and manages a pool of vm's.
    * we specify which one we want > github handles the rest
* self hosted runners.
    * in our premisses we can have our vms and thse can also be configured as runners.... Module 15/16
* ubuntu-latest # ubuntu linux
* windows-latest # widnows 
* macos-latest # mac 

### Actions
* Every cicd pipeline we need to perform basic steps. 
    * clone repo 
    * setup language runtimes
    * install depencies
    * deploy...


Action: 
* aciton is  a pre built reuslable code that performs a specifc task. 

uses: owner/repo@version

uses: actions/checkout@v7

 actions > owner/repo


actions/checkout@v7
google-github-actions/setup-cloud-sdk

Three types of actions:

composite: 
    we will using this one 

JavaScript : 

Docker:
     not recommeded  