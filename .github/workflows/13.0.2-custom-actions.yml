name: 13.0.2-Custom-Actions
on:
  workflow_dispatch:
    inputs:
      java-version:
        type: choice
        options: ['17', '21']
        default: '21'
      run-tests:
        type: boolean
        default: true

jobs:
  job1:
    name: code checkout, Build and test
    runs-on: ubuntu-latest
    steps:
      - name: code checkout
        uses: actions/checkout@v5

      - name: java setup, Test and Build
        uses: ./.github/actions/javabuild
        id: action-outputs
        with:
          java-version: ${{inputs.java-version}}
          run-tests: ${{inputs.run-tests}}
      - name: Overall Pipeline results
        run: |
          echo "${{steps.action-outputs.outputs.test-result}}"
          echo "${{steps.action-outputs.outputs.build-result}}"